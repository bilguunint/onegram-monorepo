{
  "name": "onegram-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "OneGram (1GRAM) gold-backed token — contracts, tests, deploy scripts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "grant-minter": "hardhat run scripts/grantMinter.js --network amoy",
    "test-flow:amoy": "hardhat run scripts/testFlow.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.16"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
